{
  "name": "@singha/database",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "scripts": {
    "generate": "prisma generate",
    "build": "prisma generate && tsup",
    "typecheck": "prisma generate && tsc --noEmit",
    "test": "prisma generate && vitest run",
    "test:integration": "prisma generate && vitest run",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "migrate:reset": "prisma migrate reset --force",
    "studio": "prisma studio",
    "seed": "tsx prisma/seed.ts",
    "seed:demo": "tsx prisma/seed-demo.ts",
    "seed:evolution": "tsx prisma/seed-evolution.ts",
    "seed:evolution:tx": "tsx prisma/seed-evolution-transactions.ts",
    "seed:evolution:reset": "tsx prisma/seed-evolution-reset.ts",
    "clean": "rimraf dist",
    "seed:marketplace": "tsx prisma/seed-marketplace.ts",
    "seed:marketplace:reset": "tsx prisma/seed-marketplace-reset.ts"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "@singha/contracts": "workspace:*"
  },
  "devDependencies": {
    "prisma": "^7.9.1",
    "tsx": "^4.19.2"
  }
}
